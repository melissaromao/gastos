<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Gasto</title>
    <!-- Adicionando o link para o Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
        <div class="col-md-6">
            <h1 class="text-center mb-4">Editar Gasto</h1>
            <div class="card">
                <div class="card-body">
                    <form method="POST" action="/gastos/{{gasto.id}}/editar">
                        <div class="mb-3">
                            <label for="categoria" class="form-label">Categoria:</label>
                            <input type="text" id="categoria" name="categoria" value="{{gasto.categoria}}" class="form-control" required />
                        </div>

                        <div class="mb-3">
                            <label for="valor" class="form-label">Valor:</label>
                            <input type="number" id="valor" name="valor" value="{{gasto.valor}}" class="form-control" required />
                        </div>

                        <div class="mb-3">
                            <label for="prioridade" class="form-label">Prioridade:</label>
                            <input type="text" id="prioridade" name="prioridade" value="{{gasto.prioridade}}" class="form-control" disabled />
                            <select id="prioridade" name="prioridade" class="form-control mt-2">
                                <option value="{{gasto.prioridade}}">{{gasto.prioridade}}</option>
                                <option value="Baixa">Baixa</option>
                                <option value="Média">Média</option>
                                <option value="Alta">Alta</option>
                            </select>
                        </div>

                        <button type="submit" class="btn btn-primary w-100 mb-2" style="background-color: #00875e; color: white; border: none;" >Salvar</button>
                        <a href="/" class="btn btn-link w-100" style="text-decoration: none; color: dimgray; font-weight: 600;">Voltar para meus gastos</a>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Adicionando o script do Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>